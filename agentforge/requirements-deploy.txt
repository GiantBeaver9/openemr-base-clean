# Minimal runtime for the deployed AgentForge dashboard.
# The web server, probes, and load test are stdlib-only; these three cover the
# campaign path (typed contracts + HTTP to the target). The optional LLM
# adapters use httpx directly (no SDK), and LangGraph is imported lazily, so the
# heavy/optional deps in requirements.txt are intentionally NOT installed here.
httpx>=0.27
pydantic>=2.7
jsonschema>=4.22
python-dotenv>=1.0
