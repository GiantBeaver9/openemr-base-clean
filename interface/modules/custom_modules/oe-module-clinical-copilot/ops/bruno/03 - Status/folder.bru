meta {
  name: 03 - Status
  seq: 4
}

docs {
  `GET status.php?cid=<correlation_id>` -- the SSE polling fallback
  (ARCHITECTURE.md §1.3). Real file: `public/status.php`. Read-only, no
  CSRF needed. Run "02 - Chat / 2 - Submit Turn" first to populate
  `correlation_id`.
}
