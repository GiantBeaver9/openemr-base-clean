# Build-agent protocol (read this first, every time)

You are building ONE unit of the Clinical Co-Pilot OpenEMR custom module, overnight, autonomously.

Repo root (your cwd): `/home/user/openemr-base-clean`
Module dir (M): `interface/modules/custom_modules/oe-module-clinical-copilot`

## Read before writing any code
1. `M/CONTRACTS.md` — AUTHORITATIVE names/signatures/conventions + what is already built (fact model in `M/src/Fact`, observability spine in `M/src/Observability`). Import and use those; never redefine them.
2. Repo-root spec sections for YOUR unit: `ARCHITECTURE_COMPLETE.md` (fact layer, capabilities, lab contract C1–C4, build-unit table, digest evals E1–E7), `ARCHITECTURE.md` (§1 chat, §2 verification V1–V6, §3 observability, §4 auth/PHI, §6 failure model), `USERS.md` (UC1–UC6), `docs/clinical-copilot-tradeoffs.md` (T1–T21).
3. The actual built PHP under `M/src/Fact` and `M/src/Observability` — use exact signatures.
4. Any host class you call: VERIFY it exists by reading its file under `/home/user/openemr-base-clean/src` (e.g. `src/Common/Database/QueryUtils.php`). Do not invent host APIs.

## Hard rules
- `declare(strict_types=1)`, PSR-4 `OpenEMR\Modules\ClinicalCopilot\`, 4-space indent, GPL-3 file-header docblock, native types, enums for closed sets, `readonly` value objects. Catch `\Throwable` not `\Exception`. PSR-3 context arrays. Never expose `$e->getMessage()` to users.
- Module is READ-ONLY to all core tables; writes only to `mod_copilot_*` via module repositories, using `QueryUtils` parameterized binds.
- Twig autoescape OFF: escape every sink `|text`/`|attr`/`xlt`; never `|raw` on model/user data.
- ONLY create/modify files your unit OWNS. Do NOT touch `src/Fact`, `src/Observability`, the skeleton, other units' files, or anything outside M. Do NOT run git/composer/docker/openemr-cmd (unavailable here).
- Put DB/service access behind a small interface with a Fixture impl (reads `tests/Fixtures` JSON) and a Db impl (QueryUtils/host service), so pure logic is isolated-testable with no DB.

## Validation (do this before reporting done)
- `php -l` every `.php` file you create; fix until all clean.
- Add `tests/Unit/<Name>Test.php` (global function `clinical_copilot_test_<Name>Test(): void` using the `Assert` helper — copy the pattern from `M/tests/Unit/FactModelTest.php`) for any pure logic, then run `php M/tests/run-isolated.php <Name>` (arg filters to your test). It must print all assertions passed.
- Framework-coupled files that can't run without the stack: still `php -l`; write PHPUnit-style tests under `tests/` documented as stack-required.

## Report back (concise)
Return: unit name; list of files created (relative paths); whether all `php -l` clean; your isolated test name + its final result line; and integration notes (host APIs wrapped, assumptions, anything the integrator must know). Keep it tight — the orchestrator integrates and commits.
